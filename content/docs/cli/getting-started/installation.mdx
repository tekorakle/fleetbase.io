---
title: Installing the CLI
description: Install the Fleetbase CLI globally via npm so the `flb` command is available system-wide.
---

# Installing the CLI

The Fleetbase CLI is distributed on npm as `@fleetbase/cli`. Install it globally so the `flb` command is on your PATH.

## With npm

```bash
npm install -g @fleetbase/cli
```

## With pnpm

```bash
pnpm add -g @fleetbase/cli
```

## With Yarn

```bash
yarn global add @fleetbase/cli
```

## Verify

```bash
flb --version
```

You should see something like:

```
@fleetbase/cli 0.0.6
```

## Get Help

Every command supports `--help`:

```bash
flb --help
flb scaffold --help
flb install --help
```

The top-level `flb --help` lists all available commands.

## Updating

To update an existing install:

```bash
npm install -g @fleetbase/cli@latest
# or
pnpm add -g @fleetbase/cli@latest
```

## Uninstalling

```bash
npm uninstall -g @fleetbase/cli
# or
pnpm remove -g @fleetbase/cli
```

## Next Steps

- [Install Fleetbase](/docs/cli/installation/overview) — install a self-hosted Fleetbase instance with the wizard
- [Search Extensions](/docs/cli/extensions/search) — discover extensions in the registry
- [Register an Account](/docs/cli/account/register) — create a registry account for publishing
